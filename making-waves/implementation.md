# Implementation Notes

This project uses plain HTML, CSS, and JavaScript. No build step is required.

## Directory structure

- /demos : demo HTML and JS files
- /lib   : shared library files
- Markdown files at root : docsify content

Demos are linked using absolute paths so the browser performs a full navigation.
